org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Jetifier disabled: app is fully AndroidX; Jetifier only translates legacy
# support libs and was causing "Insufficient system resources" Jetify transforms
# on Windows (see checkDebugDuplicateClasses JetifyTransform failures).
android.enableJetifier=false
kotlin.incremental=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
